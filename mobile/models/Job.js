class Job {

    constructor(id, title, shortDescription, longDescription, fromTime, toTime) {
        this.id = id
        this.title = title
        this.shortDescription = shortDescription
        this.longDescription = longDescription
        this.fromTime = fromTime
        this.toTime = toTime
    }

}

export default Job